# =============================================================================
# .env.infra — CDK / 実 AWS 環境用の設定
#
# 用途: ローカルから CDK を実行する際に使用（Bootstrap・手動デプロイ等）
# 注意: Docker Compose / LocalStack には使用しない（.env を参照）
# 管理: .gitignore 対象。実際の値はこのファイルのコピー（.env.infra）に記入
# =============================================================================

# -----------------------------------------------------------------------------
# AWS 認証情報（実アカウント用）
# AWS SSO を使う場合は `aws sso login` 後に aws configure export-credentials で取得
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# -----------------------------------------------------------------------------
# AWS アカウント ID
# CDK synthesis time にのみ使用。ECS タスクには渡さない。
# -----------------------------------------------------------------------------
DEV_AWS_ACCOUNT_ID=
PROD_AWS_ACCOUNT_ID=
AWS_REGION=ap-northeast-1

# -----------------------------------------------------------------------------
# アプリ名
# S3 バケット名・ECS リソース名などのベース名。CDK が ${APP_NAME}-{env} 等を導出。
# GitHub Actions では vars.APP_NAME で管理（ここと一致させること）
# -----------------------------------------------------------------------------
APP_NAME=myapp

# -----------------------------------------------------------------------------
# GitHub リポジトリ
# Bootstrap スタックの OIDC ロールで許可するリポジトリ。<org>/<repo> 形式で指定。
# GitHub Actions では GITHUB_REPOSITORY 変数として自動設定されるが、
# ローカルから CDK Bootstrap を実行する際はここに明示する。
# -----------------------------------------------------------------------------
GITHUB_REPO=your-org/your-repo

# -----------------------------------------------------------------------------
# ドメイン名（CloudFront / ACM 証明書に使用）
# -----------------------------------------------------------------------------
DEV_DOMAIN=dev.example.com
PROD_DOMAIN=example.com
